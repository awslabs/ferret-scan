# Grype configuration — accepted, non-actionable findings only.
# Every ignore rule MUST document WHY it is safe. Keep this list minimal;
# prefer fixing (bump the dependency / rebuild the image) over ignoring.
ignore:
  # GO-2026-5932 — golang.org/x/crypto/openpgp is unmaintained/unsafe-by-design.
  # No fixed version exists (package deprecated upstream), and ferret-scan does
  # NOT import openpgp — x/crypto is pulled transitively only for x/crypto/ocsp
  # (via pdfcpu). govulncheck confirms 0 reachable/called symbols. Nothing to
  # fix; suppressing to keep the Security tab actionable.
  - vulnerability: GO-2026-5932
