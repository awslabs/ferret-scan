name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      snapshot:
        description: "Build snapshot release"
        required: false
        default: "false"

permissions:
  contents: write
  packages: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ".go-version"

      - name: Install git-chglog
        run: go install github.com/git-chglog/git-chglog/cmd/git-chglog@latest

      - name: Generate/Update Changelog
        run: |
          if [ ! -f "CHANGELOG.md" ]; then
            echo "📝 Generating initial changelog..."
            git-chglog --output CHANGELOG.md
          else
            echo "📝 Updating changelog..."
            git-chglog --output CHANGELOG.md
          fi

      - name: Commit Changelog
        run: |
          if [ -n "$(git status --porcelain CHANGELOG.md)" ]; then
            echo "📝 Committing updated changelog..."
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add CHANGELOG.md
            git commit -m "chore: update changelog for ${GITHUB_REF_NAME:-snapshot}"
          fi

      - name: Run GoReleaser (Release)
        if: startsWith(github.ref, 'refs/tags/')
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean --skip=validate
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit binaries for pre-commit
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          # Create bin directory
          mkdir -p bin
          
          # List what GoReleaser actually created (for debugging)
          echo "GoReleaser output:"
          find dist/ -type f -name "*ferret-scan*" | head -10
          
          # Copy built binaries to bin/ with pre-commit friendly names
          # GoReleaser with formats: [binary] creates files directly in dist/
          cp dist/ferret-scan_*_linux_amd64 bin/ferret-scan-linux-amd64 2>/dev/null || echo "Linux AMD64 binary not found"
          cp dist/ferret-scan_*_linux_arm64 bin/ferret-scan-linux-arm64 2>/dev/null || echo "Linux ARM64 binary not found"
          cp dist/ferret-scan_*_darwin_amd64 bin/ferret-scan-darwin-amd64 2>/dev/null || echo "Darwin AMD64 binary not found"
          cp dist/ferret-scan_*_darwin_arm64 bin/ferret-scan-darwin-arm64 2>/dev/null || echo "Darwin ARM64 binary not found"
          cp dist/ferret-scan_*_windows_amd64.exe bin/ferret-scan-windows-amd64.exe 2>/dev/null || echo "Windows AMD64 binary not found"
          cp dist/ferret-scan_*_windows_arm64.exe bin/ferret-scan-windows-arm64.exe 2>/dev/null || echo "Windows ARM64 binary not found"
          
          # Create platform detection script
          cat > bin/ferret-scan << 'EOF'
          #!/bin/bash
          DIR="$(dirname "$0")"
          case "$(uname -s)" in
              Linux*)     
                  case "$(uname -m)" in
                      x86_64)  exec "$DIR/ferret-scan-linux-amd64" "$@" ;;
                      aarch64) exec "$DIR/ferret-scan-linux-arm64" "$@" ;;
                      *)       exec "$DIR/ferret-scan-linux-amd64" "$@" ;;
                  esac ;;
              Darwin*)    
                  case "$(uname -m)" in
                      arm64)  exec "$DIR/ferret-scan-darwin-arm64" "$@" ;;
                      *)      exec "$DIR/ferret-scan-darwin-amd64" "$@" ;;
                  esac ;;
              CYGWIN*|MINGW*) exec "$DIR/ferret-scan-windows-amd64.exe" "$@" ;;
              *)          echo "Unsupported platform: $(uname -s)" >&2; exit 1 ;;
          esac
          EOF
          chmod +x bin/ferret-scan
          
          # Commit binaries
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bin/
          git commit -m "chore: add pre-built binaries for pre-commit [skip ci]" || true
          git push || true

      - name: Run GoReleaser (Snapshot)
        if: github.event.inputs.snapshot == 'true' || (github.ref == 'refs/heads/main' && !startsWith(github.ref, 'refs/tags/'))
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: build --snapshot --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Snapshot Artifacts
        if: github.event.inputs.snapshot == 'true' || (github.ref == 'refs/heads/main' && !startsWith(github.ref, 'refs/tags/'))
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-binaries
          path: dist/*
          retention-days: 7
