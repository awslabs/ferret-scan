name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      snapshot:
        description: "Build snapshot release"
        required: false
        default: "false"

permissions:
  contents: write
  packages: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ".go-version"

      - name: Install git-chglog
        run: go install github.com/git-chglog/git-chglog/cmd/git-chglog@latest

      - name: Generate/Update Changelog
        run: |
          if [ ! -f "CHANGELOG.md" ]; then
            echo "📝 Generating initial changelog..."
            git-chglog --output CHANGELOG.md
          else
            echo "📝 Updating changelog..."
            git-chglog --output CHANGELOG.md
          fi

      - name: Commit Changelog
        run: |
          if [ -n "$(git status --porcelain CHANGELOG.md)" ]; then
            echo "📝 Committing updated changelog..."
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add CHANGELOG.md
            git commit -m "chore: update changelog for ${GITHUB_REF_NAME:-snapshot}"
          fi

      - name: Run GoReleaser (Release)
        if: startsWith(github.ref, 'refs/tags/')
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean --skip=validate
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit binaries for pre-commit
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -e

          # Create bin directory
          mkdir -p bin

          # List what GoReleaser actually created (for debugging)
          echo "=== GoReleaser output ==="
          ls -la dist/
          echo "=== Files matching ferret-scan ==="
          find dist/ -name "*ferret-scan*" -type f || echo "No ferret-scan files found"

          # Copy built binaries to bin/ with pre-commit friendly names
          # GoReleaser creates directories, so we need to look inside them
          if [[ -f "dist/ferret-scan-cli_linux_amd64_v1/ferret-scan" ]]; then
            cp "dist/ferret-scan-cli_linux_amd64_v1/ferret-scan" bin/ferret-scan-linux-amd64
            echo "✅ Copied Linux AMD64"
          fi

          if [[ -f "dist/ferret-scan-cli_linux_arm64_v8.0/ferret-scan" ]]; then
            cp "dist/ferret-scan-cli_linux_arm64_v8.0/ferret-scan" bin/ferret-scan-linux-arm64
            echo "✅ Copied Linux ARM64"
          fi

          if [[ -f "dist/ferret-scan-cli_darwin_amd64_v1/ferret-scan" ]]; then
            cp "dist/ferret-scan-cli_darwin_amd64_v1/ferret-scan" bin/ferret-scan-darwin-amd64
            echo "✅ Copied Darwin AMD64"
          fi

          if [[ -f "dist/ferret-scan-cli_darwin_arm64_v8.0/ferret-scan" ]]; then
            cp "dist/ferret-scan-cli_darwin_arm64_v8.0/ferret-scan" bin/ferret-scan-darwin-arm64
            echo "✅ Copied Darwin ARM64"
          fi

          if [[ -f "dist/ferret-scan-cli_windows_amd64_v1/ferret-scan.exe" ]]; then
            cp "dist/ferret-scan-cli_windows_amd64_v1/ferret-scan.exe" bin/ferret-scan-windows-amd64.exe
            echo "✅ Copied Windows AMD64"
          fi

          if [[ -f "dist/ferret-scan-cli_windows_arm64_v8.0/ferret-scan.exe" ]]; then
            cp "dist/ferret-scan-cli_windows_arm64_v8.0/ferret-scan.exe" bin/ferret-scan-windows-arm64.exe
            echo "✅ Copied Windows ARM64"
          fi

          # Create platform detection script
          cat > bin/ferret-scan << 'EOF'
          #!/bin/bash
          DIR="$(dirname "$0")"
          case "$(uname -s)" in
              Linux*)
                  case "$(uname -m)" in
                      x86_64)  exec "$DIR/ferret-scan-linux-amd64" "$@" ;;
                      aarch64) exec "$DIR/ferret-scan-linux-arm64" "$@" ;;
                      *)       exec "$DIR/ferret-scan-linux-amd64" "$@" ;;
                  esac ;;
              Darwin*)
                  case "$(uname -m)" in
                      arm64)  exec "$DIR/ferret-scan-darwin-arm64" "$@" ;;
                      *)      exec "$DIR/ferret-scan-darwin-amd64" "$@" ;;
                  esac ;;
              CYGWIN*|MINGW*) exec "$DIR/ferret-scan-windows-amd64.exe" "$@" ;;
              *)          echo "Unsupported platform: $(uname -s)" >&2; exit 1 ;;
          esac
          EOF
          chmod +x bin/ferret-scan

          # Show what we created
          echo "=== Created binaries ==="
          ls -la bin/

          # Check if we have any binaries to commit
          if [[ -n "$(ls -A bin/ 2>/dev/null)" ]]; then
            # Commit binaries
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"

            # Ensure we're on the main branch
            git checkout main
            git add bin/
            if git commit -m "chore: add pre-built binaries for pre-commit [skip ci]"; then
              echo "✅ Committed binaries"
              if git push origin main; then
                echo "✅ Pushed binaries to repository"
              else
                echo "❌ Failed to push binaries"
                exit 1
              fi
            else
              echo "ℹ️ No changes to commit"
            fi
          else
            echo "❌ No binaries found to commit"
            exit 1
          fi

      - name: Run GoReleaser (Snapshot)
        if: github.event.inputs.snapshot == 'true' || (github.ref == 'refs/heads/main' && !startsWith(github.ref, 'refs/tags/'))
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: build --snapshot --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Snapshot Artifacts
        if: github.event.inputs.snapshot == 'true' || (github.ref == 'refs/heads/main' && !startsWith(github.ref, 'refs/tags/'))
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-binaries
          path: dist/*
          retention-days: 7
