name: Test Release Trigger

on:
  release:
    types: [published]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test release trigger
        run: |
          echo "🎉 Release trigger works!"
          echo "Release tag: ${{ github.ref_name }}"
          echo "Event: ${{ github.event_name }}"